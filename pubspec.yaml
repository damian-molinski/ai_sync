name: ai_sync
description: CLI tool to sync AI provider configurations from a canonical source directory.
version: 1.0.0
repository: https://github.com/dm/ai_sync

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.6.0
  logging: ^1.2.0
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.26.2
